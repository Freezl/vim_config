clear
ll
mkdir /home/azureroot/.config/bash/
touch /home/azureroot/.config/bash/bash_history
clear
ll /home/azureroot/.config/bash/bashrc_local
mv /home/azureroot/.config/bashrc_local /home/azureroot/.config/bash
mv /home/azureroot/.config/bash_text-colors /home/azureroot/.config/bash
ll
clear
la
less .viminfo 
clear
ll /ect/profile
vi .bashrc 
clear
sudo -i
pwd
clear
vim .vimrc
vi .vimrc
clear
sudo -i
su
sudo vi /usr/local/RobustVPN/share/web/load/load_servercontrol_cert.html
vi .vimrc
vi /usr/local/RobustVPN/share/web/load/load_servercontrol_cert.html
sudo -i
vi .vimrc
git version
git config --global user.name "User Name"
git config --global user.name "Alex"
git config --global user.mail "reg_mysorka@mail.ru"
git config --global --list
mkdir vim_config
cd vim_config/
git clone https://github.com/Freezl/vim_config
la
la vim_config/
pwd
ll
cd ../
rm -rf vim_config/
pwd
git clone https://github.com/Freezl/vim_config
clear
ll
cd vim_config/
p ../.vimrc ./
cp ../.vimrc ./
la
git status
git add .vimrc
git commit -m "This first file"
git config --global list
git config list
git config --global --list
git config --global user.name "Freezl"
git commit -m "This first file"
git config --global --list
git commit --amend --reset-author
git push origin master
ll
la
rm README.md 
git status
git rm README.md
git commit -m "Removed README"
git push origin master
clear
la ../
la ../.config/
la ../.config/abrt/
la ../.config/bash/
cp ../.config/bash ./
